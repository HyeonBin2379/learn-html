window.onload = function () {
  // id가 header인 요소에 해당하는 dom 객체를 참조
  const header = this.document.getElementById('header');

  header.style.border = '1px solid black';
  header.style.color = 'red';
};